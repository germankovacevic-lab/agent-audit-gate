{
  "name": "agent-audit-gate",
  "version": "0.1.2",
  "license": "MIT",
  "author": "German Kovacevic <gk@agneo.app>",
  "description": "Stop your AI agent from leaking private data or obeying prompt injections on a WhatsApp line: a senior AI reviews every outbound message and holds it until it's deliberately safe to send. AI-in-the-loop audit gate / guardrail for outbound channels.",
  "type": "module",
  "scripts": {
    "build": "esbuild index.ts --bundle --format=esm --platform=node --packages=external --outfile=dist/index.js",
    "test": "node --test test/*.test.ts"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ],
    "compat": {
      "pluginApi": ">=2026.5.0",
      "minGatewayVersion": "2026.5.0"
    },
    "build": {
      "openclawVersion": "2026.5.28",
      "pluginSdkVersion": "2026.5.28"
    }
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
